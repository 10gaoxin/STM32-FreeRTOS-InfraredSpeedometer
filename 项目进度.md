# 红外测速系统 - 项目进度

最后更新：2026-04-09（速度噪声过滤 + OLED云端上传状态显示）

---

## 硬件连线步骤（最终版）

### 第一步：供电系统
```
12V电源红线 → 面包板红色电源轨（+12V）
12V电源黑线 → 面包板蓝色电源轨（GND）

供电模块：
  输入12V+ → 面包板红色轨
  输入GND  → 面包板蓝色轨
  输出3.3V → 面包板另一列红色轨（3.3V专用）
  输出GND  → 面包板蓝色轨
```

### 第二步：STM32
```
STM32 3.3V → 供电模块 3.3V输出轨
STM32 GND  → 面包板蓝色轨
```

### 第三步：ST-Link（只接3根线）
```
ST-Link SWDIO → STM32 SWDIO
ST-Link SWCLK → STM32 SWCLK
ST-Link GND   → 面包板蓝色轨
ST-Link 3.3V  → 不接
```

### 第四步：USB转TTL（CH340，串口调试）
```
CH340 TX  → STM32 PA10
CH340 RX  → STM32 PA9
CH340 GND → 面包板蓝色轨
CH340 VCC → 不接
```

### 第五步：OLED
```
VCC → STM32 3.3V
GND → STM32 GND
SCL → STM32 PB6
SDA → STM32 PB7
```

### 第六步：红外传感器
```
传感器A 发射端：棕线→12V轨，蓝线→GND轨
传感器A 接收端：棕线→12V轨，蓝线→GND轨，黑线→STM32 PA0

传感器B 发射端：棕线→12V轨，蓝线→GND轨
传感器B 接收端：棕线→12V轨，蓝线→GND轨，黑线→STM32 PA1

A和B间距100mm
```

### 第七步：ESP8266 ⚠️
```
3V3 → 供电模块 3.3V输出轨
RST → 供电模块 3.3V输出轨
EN  → 供电模块 3.3V输出轨  ← 必须接！
GND → 面包板蓝色轨
TX  → STM32 PA3
RX  → STM32 PA2

IO0 → 悬空，不接！
IO2 → 悬空，不接！
```

### 第八步：蜂鸣器（可选）
```
VCC → STM32 3.3V
GND → STM32 GND
I/O → STM32 PB0
```

---

## 当前系统状态

### 已完成 ✅
- 速度测量（TIM2+外部中断，溢出补偿已修复）
- ESP8266 MQTT连接OneNET（可正常上传数据）
- 蜂鸣器修复（低电平有效，复位不响，2秒后切回待机）
- 论文修改完成（TCP→MQTT，共15处）
- OLED验证通过（I2C地址0x78，屏幕正常显示）
- OLED自动地址检测（0x3C/0x3D，未接时系统不冻结）
- OLED_Refresh改为批量传输（每页1次I2C，共8次，原来1024次）
- I2C总线卡死自动恢复（I2C_Recovery）
- 中断优先级修复（传感器/TIM2=0，USART2=2，避免干扰计时）
- 移除启动时I2C全扫描（加快开机速度）
- OneNET物模型属性上报（thing/property/post）+ 旧版数据点双路上传
- 速度有效性校验（time_diff < 200us 丢弃，过滤传感器噪声误触发）
- OLED速度结果页新增第4行：Cloud:... → Cloud:OK / Cloud:FAIL（实时显示上传结果）

### 下次要做 ❌
- 接上ESP8266 + 红外传感器，全模块联调
  - 验证WiFi连接 → MQTT上报 → OneNET属性页面显示数据
  - 用手遮挡传感器A→B，验证测速+OLED显示+蜂鸣器+云端上传全流程

---

## 云平台配置
- 平台：OneNET CMIOT，产品ID：0GHmR8500z，设备名：stm32_speed
- MQTT Broker：mqtts.heclouds.com:1883
- Topic：$sys/0GHmR8500z/stm32_speed/dp/post/json
- WiFi：SSID=jiang，密码=12345678

---

## 下次继续
告诉 Claude："读取桌面 InfraredSpeedometer 文件夹的项目进度.md，继续帮我开发"
